DATABASE_URL="postgresql://user:password@localhost:5432/centralizador_controle?schema=public"

# VaultService (mock por env var, ver apps/api/src/vault/vault.service.ts)
# Toda Integration criada com credentialRef "env:NOME" precisa da
# variavel correspondente aqui.
# EXEMPLO_CREDENTIAL=coloque-o-token-real-aqui

# Guard placeholder da rota de ingestao (ver
# apps/api/src/common/integration-api-key.guard.ts)
INGESTION_API_KEY=troque-por-uma-chave-forte
